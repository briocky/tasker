"use client";
import {createTheme} from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: "#78123A",
    },
    secondary: {
      main: "#FFCD4B",
    },
  },
});

export default theme;